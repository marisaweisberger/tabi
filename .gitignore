# The real trip content — confirmation numbers, addresses, travel dates.
# This repo is public, so it stays out. Start from trip-data.example.js.
trip-data.js

# Python
__pycache__/
*.pyc

# Netlify build output (assembled by netlify.toml's build command)
_site/
